# 引き継ぎメモ 2026-09-06

現在の本番 BUILD_TAG：`2026-09-06an` / main は最新（Railway自動デプロイ）。
作業ツリー：`/home/claude/kinbot-main`。push は GitHub Data API 経由（package-lock.json と `#U…` は除外）。
push前は必ず `node dev/precheck.mjs`（今セッションで新設。構文/パース/HTMLタグ/BUILD_TAG/smoke を一括検査）。

## 直近セッションで入れた主な機能（2026-09-06）
- アポ獲得者：商談履歴に「アポ獲得者」セレクト。予定本文からの抽出を『所得』表記にも対応。**HTML本文から名前だけ抽出（暴走バグ修正）**。手入力は照合で消えない（`apo_setter_manual`）。「いま照合する」ボタンと「未照合割り当て」ボタンは撤去（照合は自動）。
- インセンティブ：到達通知（初実施＝¥1,000／以降5,000円ごと）＋応援の一言（各段階で文言違い）。送り先(Google Chat)ごとに on/off（`on_incentive`）。遅刻カウントは設定・管理へ。
- ダッシュボード：月次/週次トグル。週次は月選択＋「週ラップ（9/1起点7日・月内締め）」で実績は積み上げ、差分＝積み上げ実績−その週の目標。週次はインセン非表示。目標入力でリロードしない。
- リスト管理：個別ビューのアーカイブ/リサイクルカード非表示。プルダウンからも個別アーカイブ/リサイクル除去。**「リスト状況」タブ追加**（メンバー別リード数、名前タップでリスト×ステージ内訳／アーカイブ・リサイクル除外）。
- 削除の安全化：他人に割り振ったぶんは消さず owner を残った人に移す。
- 割り振り：ジャッジを含める/除くを選べる（`includeJudge`）。
- 記録の自動ルーティング（SF Statusも更新／env上書き可 RECYCLE_STAGE=89リサイクル・JUDGE_STAGE=04ジャッジ・ARCHIVE_STAGE=99アーカイブ）：
  - お断り→リサイクル、営業フォロー→ジャッジ、担当者不在5連続→リサイクル、現在使われていない/現アナ/欠番/不通→アーカイブ。
  - 現在使われていない系はかける一覧から非表示＋アーカイブのまとめに含める。
- リサイクル復活：
  - 設定マスタ `recycle_rules`（11タグ×温度/復活週/…）＋設定・管理で編集（折りたたみ）。
  - 記録モーダルに「断り理由タグ」選択。リサイクル入り時に温度をリードへ保存（結果なし=A・タグ=ルール温度）。
  - **グループ×担当の「リサイクル復活リスト」の器**（`call_lists.kind='recycle_revival'`＋group_id＋owner）。メンバー個別ビューで作成。
  - **3c 補充（唯一リードを動かす）**：記録でアーカイブ/リサイクル時、同じグループのリサイクルから温度A→B→C（同温度は古い順）で1件を、記録者の該当グループ復活リストへ即時1:1移動。復活リストは中身のリサイクルを非表示にしない。
  - 既存リサイクルの温度一括タグ付け：設定・管理→リサイクル復活ルール→試算/実行。対応表確定済み（履歴なし/不在/コールのみ/問い合わせ=A、お断り=B、受付ブロック=C、明確拒否=C、現在使われていない・アポ獲得=対象外）。実データ462件で A385/B35/C31/対象外11。
  - 診断：`/api/calls/_recyclediag`（リスト/グループ/温度別）、`/api/calls/_recyclereasons`（直近結果の文言別）。

## 次にやること（未実行・確定済み方針）
**リサイクル462件を一旦50件ずつ割り振る**（田中さん指示）:
- 相手＝インサイド全員／並び＝温度A→B→C（取れそうな順）／対象外（現在使われていない・アポ獲得）は配らない。
- 方式①（確定）：各リードを**元グループのそのメンバーの復活リスト**へ。“50件/人”は各メンバー合計50件をグループ内で温度順に配る（グループ厳守・グループ跨ぎで復活リストは分かれる）。
- まず **試算（誰に何件・重複なし・対象外除外）→ 確認 → 実行** の順。いきなり本番で動かさない。
- 実装の当たり：新エンドポイント（例 POST /api/calls/recycle-distribute {perMember:50, dryRun}）で、インサイド一覧×温度順に、元グループごとの ensureRecycleRevivalList へ moveCallTargets。
- 事前に温度の一括タグ付け（試算→実行）を済ませておくと A→B→C 順が効く。

## 継続課題（前からの積み残し）
- Recall録画が出ない（/_recdiag でS3 URL準備確認）／Recall webhook 未達の根本原因。
- Zoom Phone 連携（2アカウントどちら）。
- SF本番移行（Client ID/Secret 待ち）。
- kincall コネクタ（MCP）が田中さんのClaudeに出ない＝組織に登録済みで本人未接続。ディレクトリで検索して接続、出なければClaude組織管理者側。URL: `.../mcp` と `.../kincall/mcp`（OAuth）。
- GitHub PAT がチャット平文＝ローテーション推奨。

## 注意（今セッションの教訓・CLAUDE.mdにも記載）
- push に package-lock.json / `#U…` を混ぜない。手入力データを自動処理で消さない（_manualで保護）。カレンダー本文はHTML（<br>）＝タグ除去＋名前だけ抽出。積み上げ差分は表示値どおり。SF書込は env でステージ文字列を上書き可。push前に precheck。
