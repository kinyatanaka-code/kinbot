# 引き継ぎメモ 2026-09-10（kinbot/kincall）

## 環境（厳守）
- 作業Dir: `/home/claude/kinbot-main`／リポジトリ: `kinyatanaka-code/kinbot`（main・Railway自動デプロイ）
- 本番: `https://kinbot-production-225f.up.railway.app`
- 最新 BUILD_TAG: **`2026-09-07x`**（main `88f77e4`）
- デプロイ手順: `node dev/precheck.mjs` → `node dev/bump-version.mjs <tag>` → `server/index.js` の BUILD_TAG 更新 → `dev/kinbot-memory.md` にログ追記 → GitHub Data API で push（`package-lock.json` と `#U…` は除外）
- Claude は Railway に到達できない＝デプロイ後の画面確認は田中さん側
- 返答方針: 日本語のみ・絵文字なし・簡潔・質問は最大2件
- 教訓: `node --check` 失敗時は push しない／推測で直さず診断APIで実データ確認してから修正

## このセッションで入れたもの（すべて push 済み）
- `20260907s` 全メンバー横断のリード検索（**管理者＝田中さんだけ**）
  - `db.searchAllLeads(q,{limit})`＝`call_targets`×`call_lists` を company/person/phone/email の ILIKE で横断。list_id/list_name/list_owner/list_kind・担当者名・履歴数・最終結果を返す
  - `GET /api/calls/search-all`（`!req.isAdmin` は403、2文字未満は空）
  - 画面: リスト管理タブに「全体検索」（`data-ls="find"`、既定 hidden、`/api/me` の admin のときだけ表示）。`calls.js` の `wireFindAll`
- `20260907t` アポ一覧の「カレンダーだけ」チェック → **「カレンダーだけ作る」ボタン**に変更
  - 既存の `POST /api/smart-links/:slug/invite`（予定作成のみ・メールなし）を呼ぶ。担当未選択なら案内。確認ダイアログあり
  - 担当変更・自動割り振りの body から `noMail` 参照を除去（サーバ側の noMail 対応は残置）
- `20260907u` アポ獲得者の充足を見る診断
  - `db.apoSetterCoverage({from,to})`／`GET /api/calls/_setterdiag?from=&to=`
  - total・設定あり・未設定・手入力・自動、獲得者ごとの件数、未設定の直近10件を返す
- `20260907v` 日程変更の切り分け用診断
  - `GET /api/apo/_one?slug=` で保存されている start_time・手動で変えた時刻・event_id/invite_event_id を確認
- `20260907w`→`20260907x` **ナーチャリングの可視化**
  - 対象（最終仕様）＝**ジャッジ／営業フォローのみ**（`next_call_at` は含めない）。除外＝アポ獲得/ユーザー/失注/アーカイブ/リサイクル/死番
  - `db.NURTURE_WHERE`、`nurtureCountsByMember()`、`ensureNurtureList({owner})`（kind='nurture' の「【ナーチャリング】◯◯」を冪等作成）、`moveToNurtureLists({dryRun})`
  - `GET /api/calls/nurture`（メンバー別件数）／`GET,POST /api/calls/nurture-move`（試算・実行、実行はクローザー/管理者）
  - 画面: 月次ダッシュボード下部に「ナーチャリング（育っている見込み）」＝メンバー別カード＋合計（`loadNurture`）。設定・管理に「件数を見る／まとめる（実行）」。CSS `.kc-nur-*`

## 次にやること（合意済み・未実装）
**ナーチャリング「案A：移動するが元のリストを覚えておく」**（田中さん承認済み、見た目のモックも提示済み）
- 移送時に `call_targets` へ元リストを記録する（例：`nurture_from_id` / `nurture_from_name` を ADD COLUMN IF NOT EXISTS）
- ナーチャリングリストの一覧で、各リードに灰色バッジ「元：インターン生最新版 - 栗林」を表示（ステージの緑バッジと区別）
- 追加候補（田中さんの指定待ち）: 元リスト別の内訳表示／選んだリードを元リストへ戻すボタン／バッジ文言（「元：」で確定か）
- 背景（目的）: テレアポはナーチャリングをどれだけ貯めたかが鍵。可視化でインターンが出勤しやすくなる

## 未解決（田中さんの確認待ち・実データが必要）
1. **日程変更しても一覧の時間が変わらない**（リロード直後から変わらない）
   - 調査済み: 送信側の ISO 組み立て・ボタンとカードの slug 紐づけ（`card.querySelector` スコープ）・`updateApoStartTime` の保存・同期側の手動保護は**いずれも正しい実装**
   - 残る疑い: `/api/apo/pickup`（カレンダー取り込み）が保存値でなくカレンダー日時を出している／同じ商談名の重複レコードが表示されている
   - 次の一手: 日程変更 → `GET /api/apo/_one?slug=` で「いまの日時」を確認。変更後になっていれば pickup 側の日時決定を直す
2. **浦林さんは予備なのに、他が空いているのに割り振られる**
   - `rotation.js` は候補を `[...pri, ...rest, ...fallbacks]` の順にし、予備は最後尾＝通常メンバーが全員スキップされたときだけ選ばれる実装
   - スキップ理由は `skipped[]` に入る（`1日の上限◯件に達しています` / `isSlotFree` の理由）
   - 次の一手: `GET /api/apo/_pickdiag?start=（そのアポの日時）&business=DOC` の「飛ばされた人」の理由を確認。理由が空なのに予備が選ばれるなら並び順のバグ
3. **アポ獲得者が自動で読み取れていないのでは**（商談一覧に獲得者が出ていない）
   - 次の一手: `GET /api/calls/_setterdiag?from=2026-09-01&to=2026-09-07` の 設定あり/未設定/自動 と、未設定の例の商談名を見る
   - 見立て: 「自動」がほぼ0なら、カレンダー本文に「アポ獲得: ◯◯」が無い／商談名とカレンダー予定名の表記ゆれ
4. `20260907s`（全体検索）・`20260907t`（カレンダーだけ作る）・`20260907x`（ナーチャリング件数）の画面確認が未実施

## 継続課題（前セッションから）
- LLM 復旧方針が未決（Gemini 課金 or Claude 主へ切替）。「AI提案・ライブを止めたい」の**対象の特定が未回答**
- 「アポの取りやすいリスト」分析の軸（既存列で足りるか／SF情報も要るか）が未回答
- env `LIST_HIDE_USERS`（中澤・浦林のメール）を Railway に設定
- CSV 2000件超の DL 方針（条件分割 or SOQL 直接）
- Recall webhook 未達の根本原因（PUBLIC_URL／webhook 設定／署名検証）
- Zoom Phone 連携（2アカウントのどちらを使うか未決）
- SF 本番切り替え（SDG 側の Client ID/Secret 待ち）
- GitHub PAT のローテーション（チャットに平文で出ているため）

## 変えてはいけない決まり
- 手入力データを自動処理で消さない（`apo_setter_manual` 尊重・COALESCE 保護）
- ナーチャリング＝ジャッジ＋営業フォローのみ
- リサイクル割り振り＝グループ厳守・温度 A→B→C・アーカイブは配らない
- 記録の自動ルーティング: 現アナ等→即アーカイブ＋1:1補充／お断り→リサイクル／営業フォロー→ジャッジ／不在3連続→リサイクル（接触でリセット、env `ABSENT_TO_RECYCLE`）
- kinbot UI は絵文字を使わない。緑系トンマナ（`#0d5b47`/`#1d9e75`/`#5DCAA5`）＋インラインSVGアイコン
- 自動改善・自動学習は人の確認（PR／対応済みボタン）を挟む
