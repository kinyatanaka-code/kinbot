# 引き継ぎメモ 2026-09-12（kinbot/kincall）

※ これが最新。前回分は `dev/引き継ぎメモ_2026-09-10.md`

## 環境（厳守）
- 作業Dir: `/home/claude/kinbot-main`／リポジトリ: `kinyatanaka-code/kinbot`（main・Railway自動デプロイ）
- 本番: `https://kinbot-production-225f.up.railway.app`
- 最新 BUILD_TAG: **`2026-09-08b`**（main `6148f9c`）
- デプロイ手順: `node dev/precheck.mjs` → `node dev/bump-version.mjs <tag>` → `server/index.js` の BUILD_TAG 更新 → `dev/kinbot-memory.md` にログ追記 → GitHub Data API で push（`package-lock.json` と `#U…` は除外）
- Claude は Railway に到達できない＝デプロイ後の画面確認は田中さん側
- 返答方針: 日本語のみ・絵文字なし・簡潔・質問は最大2件
- 教訓: `node --check` 失敗時は push しない／推測で直さず診断APIで実データ確認／**CSSで要素が出ないときは position・z-index だけでなく祖先の overflow を必ず疑う**

## このセッションで入れたもの（すべて push 済み）
- `20260907s` 全メンバー横断のリード検索（**管理者＝田中さんだけ**）
  - `db.searchAllLeads(q,{limit})`／`GET /api/calls/search-all`（`!req.isAdmin` は403、2文字未満は空）
  - 画面: リスト管理タブに「全体検索」（`data-ls="find"`、admin のときだけ表示）。`calls.js` の `wireFindAll`
- `20260907t` アポ一覧の「カレンダーだけ」チェック → **「カレンダーだけ作る」ボタン**に変更
  - 既存の `POST /api/smart-links/:slug/invite`（予定作成のみ・メールなし）を呼ぶ
- `20260907u` `GET /api/calls/_setterdiag?from=&to=`（アポ獲得者の充足：設定あり/未設定/手入力/自動・未設定の例10件）
- `20260907v` `GET /api/apo/_one?slug=`（保存されている start_time・手動で変えた時刻・event_id）
- `20260907w`→`20260907x` **ナーチャリングの可視化**
  - 対象（最終仕様）＝**ジャッジ／営業フォローのみ**。除外＝アポ獲得/ユーザー/失注/アーカイブ/リサイクル/死番
  - `db.NURTURE_WHERE`／`nurtureCountsByMember()`／`ensureNurtureList({owner})`（kind='nurture' の「【ナーチャリング】◯◯」を冪等作成）／`moveToNurtureLists({dryRun})`
  - `GET /api/calls/nurture`／`GET,POST /api/calls/nurture-move`（実行はクローザー・管理者）
  - 画面: 月次ダッシュボード下部に「ナーチャリング（育っている見込み）」＝メンバー別カード＋合計（`loadNurture`）。設定・管理に「件数を見る／まとめる（実行）」。CSS `.kc-nur-*`
- `20260907y` **インセンティブの節目通知から指定の人を除外**（既定で田中欽也）
  - `checkIncentiveMilestone` 冒頭で判定。設定 `incentiveNoNotify`（配列）／env `INCENTIVE_NO_NOTIFY`（カンマ区切り、既定「田中欽也」）を `_incNorm` で正規化して突合、一致なら通知せず return（節目の記録もしない）
- `20260907z` **メニューを新構成に統一（全14ページ）**
  - 上段: ホーム／レコーディング／商談履歴／アポ履歴（apo.html）／資料トラッキング
  - `side-sep` の下に別アプリリンク（`side-app`）: kincall(`/kincall`)／Salesforce(`sf-launch.html`)／AI社員(`ai.html`)／その他ツール(`report.html`)
  - 設定は `side-foot` に `.side-set` としてログアウトの横（style.css に追加）
  - apo.html の見出しを「アポ履歴」に変更
  - 対象14ページ: home / index / history / sf-launch / report / apo / docs / settings / analysis / dashboard / deals / style-analysis / proposals / ai
- `20260908a` **アポ履歴を子メニュー化**（全14ページ）
  - `.side-wrap` + `.side-sub`（CSSは既存だが未使用だった。ホバー／フォーカスで右に展開）
  - 子項目: アポ一覧(`?tab=list`)／インターンアポ(`report.html?panel=interns`)／割り振り設定(`?tab=rot`)／チーム実績(`?tab=team`)／メール設定(`?tab=mail`)
  - `apo.js` は既に `?tab=` 対応済みでフロント改修不要
- `20260908b` **子メニューが表示されない不具合を修正**
  - 真因: `@media(min-width:761px)` の `.app { overflow: hidden; }` がサイドバーの外に開く `.side-sub` を切り取っていた
  - 対応: `.app` を `overflow: visible` に（スクロール抑制は `.main` の `height:100vh/overflow-y:auto` で従来どおり）＋ `.sidebar` の `z-index` を 3→60

## 次にやること
### 1. メニューの並べ替え（田中さんの指定待ち）
- 田中さんの依頼: 「ツールをAI社員の下に、その代わりにアポ履歴を設置させたい」
- ただし現構成では「その他ツール」は既に下段・AI社員の下にある。どの項目をどこへ動かすのか**確認中で未回答**
- 現状: 上段＝ホーム／レコーディング／商談履歴／アポ履歴／資料トラッキング、下段＝kincall／Salesforce／AI社員／その他ツール
- 有力な解釈: アポ履歴を下段（AI社員の下あたり）へ移し、その他ツールを上段の元アポ履歴の位置へ入れ替える
- 実装は python で14ページ一括置換（このセッションと同じ手順）

### 2. ナーチャリング「案A：移動するが元のリストを覚えておく」（承認済み・未実装）
- 移送時に `call_targets` へ元リストを記録（例：`nurture_from_id` / `nurture_from_name` を ADD COLUMN IF NOT EXISTS）
- ナーチャリングリストの一覧で、各リードに灰色バッジ「元：インターン生最新版 - 栗林」を表示（ステージの緑バッジと区別）。モック提示済み・見た目は承認相当
- 追加候補（指定待ち）: 元リスト別の内訳表示／選んだリードを元リストへ戻すボタン／バッジ文言

## 未解決（田中さんの確認待ち・実データが必要）
1. **日程変更しても一覧の時間が変わらない**（リロード直後から変わらない）
   - 調査済み: 送信側の ISO 組み立て・ボタンとカードの slug 紐づけ（`card.querySelector` スコープ）・`updateApoStartTime` の保存・同期側の手動保護は**いずれも正しい実装**
   - 残る疑い: `/api/apo/pickup`（カレンダー取り込み）が保存値でなくカレンダー日時を出している／同じ商談名の重複レコード
   - 次の一手: 日程変更 → `GET /api/apo/_one?slug=` の「いまの日時」を確認。変更後になっていれば pickup 側の日時決定を直す
2. **浦林さんは予備なのに、他が空いているのに割り振られる**
   - `rotation.js` は候補を `[...pri, ...rest, ...fallbacks]` の順にし、予備は最後尾＝通常メンバーが全員スキップされたときだけ選ばれる実装
   - 次の一手: `GET /api/apo/_pickdiag?start=（そのアポの日時）&business=DOC` の「飛ばされた人」の理由（`1日の上限◯件` / `isSlotFree` の理由）を確認。理由が空なのに予備が選ばれるなら並び順のバグ
3. **アポ獲得者が自動で読み取れていないのでは**
   - 次の一手: `GET /api/calls/_setterdiag?from=2026-09-01&to=2026-09-07` の 設定あり/未設定/自動 と未設定の例の商談名
   - 見立て: 「自動」がほぼ0なら、カレンダー本文に「アポ獲得: ◯◯」が無い／商談名とカレンダー予定名の表記ゆれ
4. 画面確認が未実施: `20260907s`（全体検索）／`20260907t`（カレンダーだけ作る）／`20260907x`（ナーチャリング件数）／`20260908b`（子メニューが出るようになったか。出ないならブラウザの強制再読み込みを試す）

## 継続課題（前セッションから）
- LLM 復旧方針が未決（Gemini 課金 or Claude 主へ切替）。「AI提案・ライブを止めたい」の**対象の特定が未回答**
- 「アポの取りやすいリスト」分析の軸（既存列で足りるか／SF情報も要るか）が未回答
- env `LIST_HIDE_USERS`（中澤・浦林のメール）を Railway に設定
- CSV 2000件超の DL 方針（条件分割 or SOQL 直接）
- Recall webhook 未達の根本原因（PUBLIC_URL／webhook 設定／署名検証）
- Zoom Phone 連携（2アカウントのどちらを使うか未決）
- SF 本番切り替え（SDG 側の Client ID/Secret 待ち）
- **GitHub PAT のローテーション**（チャットに平文で出ているため要再発行）

## 変えてはいけない決まり
- 手入力データを自動処理で消さない（`apo_setter_manual` 尊重・COALESCE 保護）
- ナーチャリング＝ジャッジ＋営業フォローのみ
- リサイクル割り振り＝グループ厳守・温度 A→B→C・アーカイブは配らない
- 記録の自動ルーティング: 現アナ等→即アーカイブ＋1:1補充／お断り→リサイクル／営業フォロー→ジャッジ／不在3連続→リサイクル（接触でリセット、env `ABSENT_TO_RECYCLE`）
- kinbot UI は絵文字を使わない。緑系トンマナ（`#0d5b47`/`#1d9e75`/`#5DCAA5`）＋インラインSVGアイコン
- 自動改善・自動学習は人の確認（PR／対応済みボタン）を挟む
