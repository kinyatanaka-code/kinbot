# kinbot をさわるときの決まり

このファイルは Claude Code が最初に読む。ここに書いてあることを守って直す。

## これは何のシステムか

B2B営業の「商談前 → 商談中 → 商談後」を一本につなぐ営業支援システム。
Neo Career / C Career の営業チーム（DOC・MOCHICA）が毎日使っている。
**止まると現場の仕事が止まる。** 直すときは、動いているものを壊さないことを最優先にする。

## 作りの全体像

- `server/` … Node.js（Express）。`index.js` が本体で、機能ごとにファイルが分かれている
  - `db.js`（PostgreSQL）／`salesforce.js`／`google.js`／`chat.js`／`analyzer.js`（AI）
  - `processsheet.js`（スプレッドシート）／`docs.js`（資料トラッキング）／`live.js`（ライブ配信）
  - `devnotes.js`（開発メモ）／`chatcmd.js`（Chatからの操作）
- `public/` … 画面。素のHTML/CSS/JavaScript（フレームワークなし）
- `relay/` … ライブ配信の中継サーバー（別サービスとしてRailwayに置く）
- `package.json` は `"type": "module"`。**import/export で書く**

## 書き方の決まり

1. **コメントは日本語**。「何をしているか」ではなく「**なぜそうしたか**」を書く。
   使う人も読むので、専門用語は避けてやさしく書く。
2. 画面に出す文字も日本語。エラーは「何が起きたか」「どうすればいいか」を書く。
3. **絵文字のアイコンは使わない**。画面のアイコンはインラインSVGで、kinbotの緑
   （`#0d5b47` / `#1d9e75` / `#5DCAA5`）に合わせる。丸みのある形にする。
   （Google Chatに送る文章の中の絵文字は使ってよい）
4. 画面は**カード型**でつめて並べる。操作は小さなアイコンで、押す先が分かるように色を付ける。
5. 既存の書き方に合わせる。新しいライブラリは足さない（依存を増やさない）。

## 直したら BUILD_TAG も更新する

`server/index.js` の `BUILD_TAG` と `BUILD_FEATURES` は、
「いま本番で動いているのは、いつの版か」を確かめるための印です。
機能を足したら**必ずここも書き換えてください**。

書き換えを忘れると、画面やAPIには古い日付が出たままになり、
「デプロイされていないのでは」と誤解する原因になります。

## 直すときの手順

1. 変える前に、関係するファイルを読んで、いまの動きを理解する。
2. 変更は**小さく**。1つの要件につき、必要な最小限だけ変える。
3. 必ず確かめる。
   ```
   node --check server/index.js     # 変えた .js すべて
   RECALL_API_KEY=dummy SESSION_SECRET=dummy GEMINI_API_KEY=dummy PORT=8199 node server/index.js
   ```
   起動して `[apo-scan] 自動スキャンの間隔` まで出れば、とりあえず動いている。
4. データベースの列を足すときは `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` を使う。
   **既存のデータを消す変更はしない。**
5. 消す・戻せない操作（Salesforceのコンバート、カレンダーの削除、DELETE）は、
   必ず確認をはさむか、対象を絞る。

## やってはいけないこと

- `node_modules` や `package-lock.json` を上げる
- 環境変数の値（鍵・トークン）をコードに書く、ログに出す
- 大きな作り替え（フレームワーク導入・全面書き直し）
- テストしていない状態で `main` に直接push

## 迷ったら

自信が持てない要件は**手を付けず**、PRの本文に「これは判断が必要」と書いて残す。
勝手に仕様を決めない。

## 用語

| 言葉 | 意味 |
|---|---|
| アポ | 商談の予定。インサイドが取り、クローザーが担当する |
| 期内・期外 | アポを取った月と商談の月が同じなら「期内」 |
| 立ち上げ | Salesforceでリードをコンバートして商談を作ること |
| プロセスシート | 架電結果を書き込むスプレッドシート |
| スマートリンク | 担当が変わってもURLが変わらない共有Zoom URL |
