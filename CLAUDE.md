# kinbot をさわるときの決まり

このファイルは Claude Code が最初に読む。ここに書いてあることを守って直す。

## まず「記憶」を読む（必須）

`dev/kinbot-memory.md` を読むこと。ここには、このプロジェクトの経緯・田中さんの指示・
これまでの決めごとがまとまっている。直すときは、その前提を踏まえて動く。
**新しい決定や指示に気づいたら、`dev/kinbot-memory.md` のログに1行足す**（AI社員が次から覚えていられるように）。

## これは何のシステムか

B2B営業の「商談前 → 商談中 → 商談後」を一本につなぐ営業支援システム。
Neo Career / C Career の営業チーム（DOC・MOCHICA）が毎日使っている。
**止まると現場の仕事が止まる。** 直すときは、動いているものを壊さないことを最優先にする。

## 作りの全体像

- `server/` … Node.js（Express）。`index.js` が本体で、機能ごとにファイルが分かれている
  - `db.js`（PostgreSQL）／`salesforce.js`／`google.js`／`chat.js`／`analyzer.js`（AI）
  - `processsheet.js`（スプレッドシート）／`docs.js`（資料トラッキング）／`live.js`（ライブ配信）
  - `devnotes.js`（開発メモ）／`chatcmd.js`（Chatからの操作）
- `public/` … 画面。素のHTML/CSS/JavaScript（フレームワークなし）
- `relay/` … ライブ配信の中継サーバー（別サービスとしてRailwayに置く）
- `package.json` は `"type": "module"`。**import/export で書く**

## 書き方の決まり

1. **コメントは日本語**。「何をしているか」ではなく「**なぜそうしたか**」を書く。
   使う人も読むので、専門用語は避けてやさしく書く。
2. 画面に出す文字も日本語。エラーは「何が起きたか」「どうすればいいか」を書く。
3. **絵文字のアイコンは使わない**。画面のアイコンはインラインSVGで、kinbotの緑
   （`#0d5b47` / `#1d9e75` / `#5DCAA5`）に合わせる。丸みのある形にする。
   （Google Chatに送る文章の中の絵文字は使ってよい）
4. 画面は**カード型**でつめて並べる。操作は小さなアイコンで、押す先が分かるように色を付ける。
5. 既存の書き方に合わせる。新しいライブラリは足さない（依存を増やさない）。

## 画面（CSS/JS）を直したら、版の印も更新する

`public/*.html` の `style.css?v=...` と `*.js?v=...` の数字は、
ブラウザに「新しいファイルを取りに行って」と伝えるための印です。
**見た目や画面の動きを直したら、必ずこの版を上げてください**（例：20260818a → 20260818b）。

`dev/bump-version.mjs` を実行すると、全画面の版をまとめて上げられます。

これを忘れると、直したのに古い画面が出たまま「変わってない」と言われます。

## 直したら BUILD_TAG も更新する

`server/index.js` の `BUILD_TAG` と `BUILD_FEATURES` は、
「いま本番で動いているのは、いつの版か」を確かめるための印です。
機能を足したら**必ずここも書き換えてください**。

書き換えを忘れると、画面やAPIには古い日付が出たままになり、
「デプロイされていないのでは」と誤解する原因になります。

## 直すときの手順

1. 変える前に、関係するファイルを読んで、いまの動きを理解する。
2. 変更は**小さく**。1つの要件につき、必要な最小限だけ変える。
3. 必ず確かめる。
   ```
   node --check server/index.js     # 変えた .js すべて
   RECALL_API_KEY=dummy SESSION_SECRET=dummy GEMINI_API_KEY=dummy PORT=8199 node server/index.js
   ```
   起動して `[apo-scan] 自動スキャンの間隔` まで出れば、とりあえず動いている。
4. データベースの列を足すときは `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` を使う。
   **既存のデータを消す変更はしない。**
5. 消す・戻せない操作（Salesforceのコンバート、カレンダーの削除、DELETE）は、
   必ず確認をはさむか、対象を絞る。

## やってはいけないこと

- `node_modules` や `package-lock.json` を上げる
- 環境変数の値（鍵・トークン）をコードに書く、ログに出す
- 大きな作り替え（フレームワーク導入・全面書き直し）
- テストしていない状態で `main` に直接push

## 迷ったら

自信が持てない要件は**手を付けず**、PRの本文に「これは判断が必要」と書いて残す。
勝手に仕様を決めない。

## 用語

| 言葉 | 意味 |
|---|---|
| アポ | 商談の予定。インサイドが取り、クローザーが担当する |
| 期内・期外 | アポを取った月と商談の月が同じなら「期内」 |
| 立ち上げ | Salesforceでリードをコンバートして商談を作ること |
| プロセスシート | 架電結果を書き込むスプレッドシート |
| スマートリンク | 担当が変わってもURLが変わらない共有Zoom URL |

## push前の精度チェック（必ず回す）

編集したら、pushの前に **`node dev/precheck.mjs`** を1回走らせる。
サーバJSの構文（node --check）／フロントJSのパース／HTMLタグの開閉数／BUILD_TAGの有無／smoke をまとめて検査する。
FAIL が1つでも出たら push しない。WARN（package-lock.json・文字化け名 #U… の存在）は push に含めない合図。

## この開発で学んだ「精度アップの決まり」

過去に実際にやらかした失敗と、その再発防止。**必ず守る。**

- **push に混ぜない**：`package-lock.json`（`npm install` の副産物）と、zip展開で文字化けした `#U…` のファイル名は push に含めない。GitHub Contents/Data API で上げるときは除外する。誤って上げたら、削除コミットで戻す。
- **手入力データを自動処理で消さない**：自動で「全消し→やり直し」する処理（例：アポ獲得者の照合＝clearApoSetters）は、**手入力の印（`_manual` 列）を付けて対象から外す**。印の無い一括クリアは既存の手入力を消す。照合の実行を人に勧める前に、それが手入力を消さないか確認する。
- **カレンダー本文はHTML**：Googleカレンダーの説明は改行が `<br>` で、本物の改行が無い。`[^\n\r]+` で行末まで取ると本文全部を拾って暴走する。**先に `<br>→改行`・タグ除去してから**抽出し、名前だけ取る（長さ・URL・HTML混入で無効化）。表示側も同じくHTML→テキスト化してから出す。
- **文字列抽出は必ず上限とサニタイズ**：名前や短い値を正規表現で取るときは、長さ上限・区切り（`<`・改行・次項目ラベル）・URL/HTML混入の除外を入れる。通知など人目に出る値は特に。
- **積み上げ表示の一貫性**：カードに「実績」「目標」「差分」を出すなら、**差分＝表示している実績−表示している目標**にする。裏で別の値（合計目標など）で引くと「差分が全然違う」になる。
- **AIの出力上限（MAX_TOKENS）**：Geminiは思考トークンが出力枠を食う。構造化JSON抽出（要約・段階分け）では 2.5-flash 系で `thinkingConfig.thinkingBudget:0`、出力枠は余裕を持たせる。フォールバック（Groq等）は小さいTPMで429/413しやすいので、まずGeminiで完了させる。
- **str_replace 後は必ず再確認**：置換で `try` や関数の一部が消えることがある。編集後は `node --check`（＝precheck）で構文を確認してから進む。
- **トークンは履歴に残さない・使い終わったらローテーション**：チャットに貼られた本番トークンは平文で残る。作業後に無効化・再発行を促す。
